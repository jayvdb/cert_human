language: python
install: "make"
script:
  - make ci
cache: pip
jobs:
  include:
    - stage: test
      script:
        - make ci
      python: '2.7'
    - stage: test
      script:
        - make ci
      python: '3.7'
    - stage: coverage
      python: '3.7'
      script: codecov
